from setuptools import setup

setup(
    name="execmd", 
    version="0.1.0", 
    description="Execute Command (execmd) simply executes a specified command on the command line and reports results.",
    url="https://github.com/clesnar/execmd",
    author="Colin Lesnar",
    author_email="Colin.Lesnar@live.com",
    license="MIT License",
    packages=["execmd"],
    install_requires=["argparse"],
    # classifiers=["",]
)